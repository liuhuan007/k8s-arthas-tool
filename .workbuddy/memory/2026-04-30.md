# 2026-04-30

- 修复 Pod 监控/Arthas 启动相关问题：删除 api/pod_apis.py 中误加的重复 `/api/monitor/pod` 路由，保留 server.py 的监控端点；让 `app-ui.js` 和 `static/js/components/monitor.js` 的进程渲染兼容 `container_metrics.processes` 与 Pod 诊断 `processes` 两种数据结构；修复 Arthas MCP 检查误用本地 port-forward 端口，应使用 Pod 内 `arthas_http_port`；修复 `list_pod_connections` 读取不存在的 `arthas_conn.mcp_available` 属性，改读连接 entry；port-forward 超时后释放已分配端口；新增 `tests/test_pod_monitor_and_arthas.py` 回归测试。验证：`python -m pytest tests -q` 29 passed，关键 Python 文件 `py_compile` 通过。
- 创建正式 PRD `docs/k8s-arthas-diagnostic-workbench-prd.md`：整合当前连接重构设计、工具链中心、任务中心、Arthas 最佳实践、AI 全局助手和分阶段路线图。DeepSeek 分享链接当前 WebFetch 只能抓到 `DeepSeek` 标题，无正文；PRD 中明确保留为外部参考和待补录设计区，避免臆造不可访问内容。
- 根据用户粘贴的《Arthas on Kubernetes 智能运维调试平台 PRD v4.0》，覆盖整合生成 PRD v4.1：保留工行“平台-网关-进程”三层架构思想、金融级安全、诊断闭环、热修复审批、多用户协作控制等内容；结合当前项目实际，将 React/JWT/SQLAlchemy/Kubernetes SDK/Tunnel Server 必选等调整为目标演进或可选增强，当前基线仍为 Flask + SQLite + kubectl + 原生前端。
- 根据 PRD v4.1 拆分实施任务，生成 `docs/plans/2026-04-30-arthas-k8s-workbench-task-breakdown.md`。计划按 P0/P1/P2、Epic、Task、Sprint 拆分，覆盖连接管理、Arthas 生命周期、Pod 监控、线程/方法诊断、工具链、任务中心、安全审计、热修复、AI 报告、Tunnel 和批量诊断。并在会话任务列表中创建 Sprint 1 的 P0-1.1、P0-1.2、P0-1.3、P0-2.1。
- 按用户要求调整 Sprint 3：明确“不加入升级能力”，保留工具分发但不做工具/Arthas 版本升级闭环；新增 Namespace 级账号授权为核心任务，要求明确到哪个账号可操作哪个集群下哪些 namespace。已更新 PRD 安全体系和任务计划，新增 P0-7.1~P0-7.5（user_namespaces 表、授权 API、统一校验服务、用户管理 UI、授权审计），并创建对应会话任务。
- 完成 Sprint 3 namespace 授权前端闭环：在 `static/user-management.html` 新增 Namespace 授权模态框；在 `static/js/user-management.js` 用户表增加“管理namespace”入口，接入 `/api/user-namespaces/*` 授权/取消授权接口，并支持按集群加载 namespace 候选与 `*` 全量授权。新增 `tests/test_user_management_namespace_frontend.py` 先失败后通过。验证：`python -m pytest e:/tmp/k8s-arthas-tool/tests -v --tb=short`，38 passed。
- 修复普通用户 seeyon 登录后反复弹出“只有管理员可以访问此页面”的问题。根因是 `index.html` 初始加载时直接加载了用户管理/审计日志 iframe，普通用户即使看不到菜单也会执行 iframe 内 admin 校验并弹窗跳转。修复：用户管理/审计日志 iframe 改为 `data-src` 懒加载，仅 admin 切换到对应 tab 时由 `loadAdminFrameIfNeeded` 设置 src；`switchTab` 对非 admin 访问管理 tab 做拦截；嵌入式 `user-management.html`/`audit-logs.html` 对非 admin 不再 alert，仅回父页面连接中心。新增 `tests/test_non_admin_admin_frame_guard.py`。验证：全量测试 40 passed。
