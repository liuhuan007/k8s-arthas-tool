# 2026-05-08

## 连接中心 TTL 功能实现

- `connections` 表新增 `ttl_hours` 字段（默认 0 = 不自动过期），`db.py` 迁移脚本已添加
- `ConnectionStateManager._run_ttl_cleanup()` 改为按每条连接的 `ttl_hours` + `last_active_at` 判断过期（原硬编码 24h）
- `server.py` 的 `/api/arthas/connect` 接口新增 `ttl_hours` 请求参数，建连时写入 DB
- `server.py` 的 `/api/arthas/exec` 接口每次执行命令时更新 `last_active_at`
- 自动重连路径（`_ensure_connection`）也同步更新 `last_active_at`
- 架构设计文档 `2026-05-06-architecture-design.md` 已更新：新增 3.9 连接有效期（TTL）章节，数据模型 7.2 区分 P0/P1 表

## 架构设计文档 P0/P1 分界

- 6 张表可先不做（全部 P1）：`capability_versions`、`schema_version`、`diagnosis_cases`、`metric_baselines`、`anomaly_rules`、`anomaly_events`
- 属于"智能化"能力（异常感知、知识沉淀、能力版本管理），P0 只需核心诊断 + 连接管理 + 执行记录

## 设计文档整合

- 5月6日已完成将 3 份 5月4日文档（诊断中心v2、任务中心重构、架构评审改进）合并为 `2026-05-06-architecture-design.md`，源文件已删除
- 当前 specs 目录保留 3 份文档：连接交互重构、系统设计、综合架构设计
