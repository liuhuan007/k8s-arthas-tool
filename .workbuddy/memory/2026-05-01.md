# 2026-05-01

- 修复 namespace 授权两处问题：1）管理员授权列表原本显示 `cluster_id / namespace`，如 `e272008a / seeyon-performance`，现改为通过集群 id->name 映射显示环境名称；2）普通用户已授权 namespace 后 `/api/pod/connect` 仍 403，根因是前端连接传 `cluster_name`（如“性能环境”），而 `user_namespaces` 存 `cluster_id`（如 `e272008a`），`AuthorizationService.can_access_namespace/filter_namespaces` 未兼容 id/name。已改为复用 `_cluster_keys()` 同时匹配 id/name。新增 `tests/test_namespace_authorization_cluster_name_compat.py`，全量验证 `python -m pytest e:/tmp/k8s-arthas-tool/tests -v --tb=short`：43 passed。
- 修复“清理失效连接”与连接中心 namespace 权限选择不严谨：清理按钮原先只删除前端 localStorage 并异步调用 `/api/arthas/disconnect`，Pod 连接/数据库残留可能未清理；新增 `/api/arthas/connections/cleanup-stale`，统一断开 Arthas 内存连接、调用 `app.cleanup_pod_connection_by_id` 清理 Pod 连接，并删除 `connections` 表记录。连接中心 namespace 从可手输 input 改为严格 select；`populateNsList` 只填充后端过滤后的授权 namespace，`normalizeActiveNamespace` 自动纠正默认值，`validateSelectedNamespace` 防止非授权 namespace 连接/加载 Pod。新增 `tests/test_connection_cleanup_and_namespace_strictness.py`，全量验证 45 passed。
- 修复连接中心 Pod 切换状态问题：`onPodSel` 现在首次选择 Pod 后自动选择第一个 container；新增 `resetConnectionFlowForTargetChange`，在切换集群/Pod 时清理 `_currentConnId`、旧连接态、Java PID 检测结果、运行时信息和后续步骤状态，按钮恢复为“Pod 连接”；`getCurrentPodTarget` 在用户手动切换目标后优先使用表单目标，避免后续连接/监控/诊断继续使用上一个 Pod。`two-step-connection.js` 新增 `resetTwoStepConnectionState` 并在 Pod 连接成功后清理 manual dirty。新增 `tests/test_connection_center_target_reset.py`，全量验证 49 passed。
